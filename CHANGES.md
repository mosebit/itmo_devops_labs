### Описание репозитория
В качестве spark-скрипта используется адаптированный скрипт из прошлой лабораторной работы: получает страницу свежих новостей (extract) -> вытаскивает ссылки на конкретные посты (transform) -> сохраняет (load)

Отработавший DAG:
![alt text](<dag_new.png>)

Задача в веб-интерфейсе Spark:
![alt text](<spark_web.png>)

### Изменения относительно первой лабораторной работы

1. DAG переведен с выполнения отдельных шагов на вызов Spark-джобы через BashOperator (с SparkSubmitOperator возникли проблемы в виде несовестимости `apache-airflow-providers-apache-spark` c Airflow 3.2.0)
2. Реализована отдельная Spark-джоба в ./spark/smartlab_links_job.py

